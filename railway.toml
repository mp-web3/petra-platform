# Railway configuration for API service
# For multiple services, configure each service separately in Railway UI

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.api"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
