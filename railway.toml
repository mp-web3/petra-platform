[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
buildContext = "."

[deploy]
startCommand = "sh -c 'cd apps/api && pnpm prisma:deploy && node dist/main.js'"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"

